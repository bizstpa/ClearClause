{
  "name": "clearclause",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:extension": "tsc --noEmit -p extension/tsconfig.json && vite-node scripts/build-extension.ts",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval": "vite-node scripts/eval.ts",
    "eval:misses": "vite-node scripts/eval-misses.ts",
    "fetch": "vite-node scripts/fetch-policy.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vite-node": "^3.2.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  }
}
