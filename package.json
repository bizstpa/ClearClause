{
  "name": "clearclause",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval": "vite-node scripts/eval.ts",
    "eval:misses": "vite-node scripts/eval-misses.ts",
    "fetch": "vite-node scripts/fetch-policy.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.3.0",
    "vite-node": "^3.2.0",
    "vitest": "^3.1.0"
  }
}
